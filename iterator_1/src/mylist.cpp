#include "mylist.h"

// mylist

// template <typename T>
// void mylist<T>::push_back(const T &elem)
// {
//     mylist_node<T> *newNode = new mylist_node<T>(elem, nullptr);
//     if (!head)
//         head = newNode;
//     else
//         head->next = newNode;
//     tail = newNode;
// }
